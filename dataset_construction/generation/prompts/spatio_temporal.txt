You are phrasing a multiple-choice spatio-temporal reasoning question for a multi-camera video benchmark.

You are given a grounding context (either a spatial state or a temporal event) and a FINAL set of answer options. Your ONLY job is to render fluent, natural language. You must NOT add, remove, reorder, or change the meaning of any option, and you must NOT change which option is correct.

Rules:
- Write a single, cogent question that cross-references space and time: given the grounding spatial state, ask which event happens after it; or given the grounding event, ask the spatial relationship that holds at that time.
- Ground descriptions in object appearance and activity, not bare class labels.
- Keep each option's meaning identical to what is provided; you may only smooth wording.
- Return the options in the SAME order and with the SAME letters they are given.

Structured facts:
{facts}

Final options (keep order and letters):
{options}

Return ONLY JSON in this exact form:
{{"question": "<the question text>", "options": {{"A": "<rendered A>", "B": "<rendered B>", "C": "<rendered C>", "D": "<rendered D>"}}}}
