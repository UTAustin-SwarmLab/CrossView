You are writing the reference summary for a multi-camera video, used as ground truth in a benchmark.

You are given a chronological timeline of grounded events extracted from a spatio-temporal scene graph, including which camera(s) observed each event. Write a comprehensive yet concise summary (a few sentences) that:
- accounts for the primary interactions and how the scene evolves over time,
- spans all available camera perspectives rather than a single view,
- stays strictly faithful to the timeline: do NOT invent events, objects, or outcomes not present below, and do NOT omit major events.

Timeline:
{facts}

Return ONLY the summary text, with no preamble, headings, or commentary.
