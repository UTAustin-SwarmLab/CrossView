You are phrasing a multiple-choice event-ordering question for a multi-camera video benchmark.

You are given a shuffled list of events and a FINAL set of answer options, where each option is a candidate chronological ordering. Your ONLY job is to render fluent, natural language. You must NOT add, remove, reorder, or change the meaning of any option, and you must NOT change which option is correct.

Rules:
- Write a single question instructing the reader to put the listed events into the order they occur in the multi-camera video.
- Ground event descriptions in object appearance and activity, not generic labels.
- Keep each option's ordering identical to what is provided; you may only smooth wording of the event phrases.
- Return the options in the SAME order and with the SAME letters they are given.

Structured facts:
{facts}

Final options (keep order and letters):
{options}

Return ONLY JSON in this exact form:
{{"question": "<the question text>", "options": {{"A": "<rendered A>", "B": "<rendered B>", "C": "<rendered C>", "D": "<rendered D>"}}}}
